specVersion: "v1"
serviceId: "task-manager-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "task-manager-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
